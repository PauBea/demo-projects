import { createContext } from "react";

export const AdminProductsContext = createContext(null);
export const AdminOrdersContext = createContext(null);
export const AdminCategoriesContext = createContext(null);